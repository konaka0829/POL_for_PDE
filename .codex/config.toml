# Project-scoped defaults for this repo

model = "gpt-5.3-codex"
model_reasoning_effort = "high"

# Safer defaults; you can still use --full-auto for fast iteration.
approval_policy = "on-request"
sandbox_mode = "workspace-write"

# Optional: avoid surprises from web search during coding (uncomment if you want)
# web_search = "disabled"
